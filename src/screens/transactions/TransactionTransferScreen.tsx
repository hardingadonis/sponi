import { Text, View } from 'react-native';

const TransactionTransferScreen = () => {
	return (
		<View>
			<Text>TransactionTransferScreen</Text>
		</View>
	);
};

export default TransactionTransferScreen;
